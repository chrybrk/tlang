statement	-> { let, } | call | expr -> `;`
let				-> ident: primary = expr
call			-> ident(expr)
expr 			-> term([+, -]term)*
term 			-> factor([*, /]factor)*
factor 		-> (expr) | primary
primary		-> number | ident



- SIMD
- module
- helpful error and warning
